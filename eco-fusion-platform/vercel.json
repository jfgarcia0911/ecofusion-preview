{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "npm install --include=dev",
  "buildCommand": "npx prisma generate && node scripts/migrate.js && next build",
  "regions": [
    "hnd1"
  ]
}
