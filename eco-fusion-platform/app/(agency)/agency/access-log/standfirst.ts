/**
 * The Access Log's opening sentence, shared by the page and its skeleton so
 * the two cannot say different things and swap one for the other on arrival.
 */
export const ACCESS_LOG_STANDFIRST =
    "Your agency's sign-ins, every time somebody stepped into one of its businesses from above - your team or EcoFusion support - and every change made while inside. Written by the app and never deleted by it.";

/** The same, for EcoFusion's console, which reads every agency's. */
export const CONSOLE_ACCESS_LOG_STANDFIRST =
    "Every agency's trail in one place: sign-ins, every visit to a business from above, and every change made to a business, an agency or the platform itself, the EcoFusion admin's included.";
