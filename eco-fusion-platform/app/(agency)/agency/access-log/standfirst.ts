/**
 * The Access Log's opening sentence, shared by the page and its skeleton so
 * the two cannot say different things and swap one for the other on arrival.
 */
export const ACCESS_LOG_STANDFIRST =
    "Every time EcoFusion staff stepped into a customer's business, every change made while inside one, and every change to the platform itself, including the master account's. Written by the app and never deleted by it.";
