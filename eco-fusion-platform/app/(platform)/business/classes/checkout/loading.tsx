import { CheckoutSkeleton } from "@/components/skeletons/PageSkeletons";

/** Shaped like the checkout that is coming, so nothing moves when it lands. */
export default function Loading() {
    return <CheckoutSkeleton />;
}
