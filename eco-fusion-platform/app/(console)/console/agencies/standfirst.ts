/**
 * The Agencies page's opening sentence, shared with its skeleton so the two
 * cannot say different things and swap one for the other on arrival.
 */
export const AGENCIES_STANDFIRST =
    "EcoFusion's customers. Each agency has one trial and one subscription, covering the businesses its plan allows. Open one to support it; set one up, change its plan or extend its trial.";
